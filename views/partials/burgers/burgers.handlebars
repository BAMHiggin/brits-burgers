<li>
	{{id}}.{{burger_name}}

	<button class="change-devour" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#unless devour}}DEVOUR{{/unless}}
	</button>


</li>


{{!-- data-newDevour="{{devour}}" --}}
